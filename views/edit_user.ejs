<form action="/update/<%=user._id%>" method="POST" class="my-form">
    <link rel="stylesheet" href="/stylesheets/input.css"> 

  <form action="/update/<%=user._id%>" method="POST" class="my-form">
    <link rel="stylesheet" href="/stylesheets/input.css"> 

  <div class="form-group">
    <label>Name</label>
    <input value="<%= user.wickets %>" type="number" name="name">
  </div>

  <div class="form-group">
    <label>Age</label>
    <input value="<%= user.overs %>" type="number" name="age">
  </div>

  <div class="form-group">
    <label>Height (cm)</label>
    <input value="<%= user.wickets %>" type="number" name="height">
  </div>

  

  <div class="form-group">
    <label>Comments</label>
    <textarea name="comments"><%= user.notes %></textarea>
  </div>

  <button type="submit" class="submit-btn">Update User</button>

</form>

  <div class="form-group">
    <label>Comments</label>
    <textarea name="comments"><%= user.comments %></textarea>
  </div>

  <button type="submit" class="submit-btn">Update User</button>

</form>