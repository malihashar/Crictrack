<html>
    <head>
        <title> Cric Track</title>
        <link rel="stylesheet" href="/stylesheets/input.css">
    </head>
    <header>
        <div class="logo">
         <h2>
          Cric Track
         </h2>
        </div>
        <div class="buttons">
        <a class="ct" href=""><button>Stats</button></a>
        <a class="cta" href=""><button>Profile</button></a>
        </div>
       
    </header>
    <body>
        <div class="players">
  <% users.forEach(user => { %>
    <button class="player-card" href="/d/<%= user._id %>">
      <div class="player-name"><%= user.name %></div>
      <div class="player-position"><%= user.position %></div>
      <a class="delete-button" href="/delete/<%= user._id %>">Delete</a>
      
    </button>
  <% }) %>
</div>

        <a class="input" href="/b"><button>New player</button></a>
        <div class="circle"></div>

    </body>
</html>